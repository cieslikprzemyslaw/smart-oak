import styled from 'styled-components';

const PatroniteSection = styled.section`
    position: relative;
    display: flex;
    justify-content: space-around;
    align-items: space-around;
    flex-wrap: wrap;
    width: 100%;
    z-index: 5;
    background-color: #F7F6F5;
`;

const Logo = styled.img`
    height: 75px;
`;

export {PatroniteSection, Logo};